Docker run parameter -e



Manager script må ha:
	finne ip til server (factor)
	max 10 docker script
		hvis mer enn 10, lag en ny server
	ssh til server1 og 2



først skjekk container en, hvis det er mer enn 10, gå til neste server.
Hvis det ikke finnes en neste server, lag da en ny maskin, ssh inn på den, og start en container


hardlimit på hvor mange servere det er mulig å lage


lage array med alle serverene

mappenavn som parameter

